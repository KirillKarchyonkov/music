export default function AsideSectionTitle({children})  {

    return (
        <h2 style={{
            fontWeight: "300",
            fontSize: "12px",
            color: "#EE10B0",
            opacity: "0.6",
            margin: "24px 0 0 0"
            }}>
            {children}
        </h2>
    )
}